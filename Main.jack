class Main {
    function void main () {
        var BasedSquare a;
        var Point point;
        let point = Point.new(16, 15);
        let a = BasedSquare.new(point);
        do Output.printInt(a.getSquareX());
        return;
    }
}